## What this changes

One or two sentences. What is different afterwards, and why it needed to be.

## How it was checked

Paste the output rather than describing it. A claim that the tests pass is not
evidence that they did.

```text
```

- [ ] `ruff format --check .` and `ruff check .` are clean
- [ ] Every test file runs, and coverage is 100% of statements and branches
- [ ] `python3 -m sdsp.doctor` reports nothing on this machine
- [ ] `conformance/corpus.py` was run and every case still agrees

## If this changes what the part does

The reference is the authority, and a disagreement with it is a finding rather
than a preference. A change to what the part produces has to say which case,
what it produced before, and what it produces now, because a number that came
from nowhere is a number nobody can act on.

## What it does not carry

- [ ] No samples, no music, and no bytes from either
- [ ] Nothing that says where to obtain them
